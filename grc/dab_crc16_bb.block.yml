# auto-generated by grc.converter

id: dab_crc16_bb
label: CRC16
category: '[DAB]'

parameters:
-   id: length
    label: Length
    dtype: int
-   id: generator
    label: Generator polynome
    dtype: raw
-   id: initial_state
    label: Init state
    dtype: raw

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import gnuradio.dab as dab
    make: dab.crc16_bb(${length}, ${generator}, ${initial_state})

file_format: 1
