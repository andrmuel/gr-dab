# auto-generated by grc.converter

id: dab_select_subch_vfvf
label: Select subchannel
category: '[DAB]'

parameters:
-   id: vlen_in
    label: Vector length in
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vlen_out
    label: Vector length out
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: address
    label: Address
    dtype: raw
-   id: total_size
    label: Total size
    dtype: raw

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen_in }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen_out }

templates:
    imports: import gnuradio.dab as dab
    make: dab.select_subch_vfvf(${vlen_in}, ${vlen_out}, ${address}, ${total_size})

file_format: 1
