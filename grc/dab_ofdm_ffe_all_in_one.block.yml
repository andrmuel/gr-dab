# auto-generated by grc.converter

id: dab_ofdm_ffe_all_in_one
label: OFDM ffe all in one
category: '[DAB]'

parameters:
-   id: symbol_length
    label: Symbol length
    dtype: int
-   id: fft_length
    label: FFT length
    dtype: int
-   id: num_symbols
    label: Number of symbols
    dtype: int
-   id: alpha
    label: alpha
    dtype: real
-   id: sample_rate
    label: Sample rate
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.dab as dab
    make: dab.ofdm_ffe_all_in_one(${symbol_length}, ${fft_length}, ${num_symbols},
        ${alpha}, ${sample_rate})

file_format: 1
