# auto-generated by grc.converter

id: dab_diff_phasor_vcc
label: Diff Phasor VCC
category: '[DAB]'

parameters:
-   id: num_carriers
    label: num_carriers
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ num_carriers }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ num_carriers }

templates:
    imports: import grdab
    make: grdab.diff_phasor_vcc(${num_carriers})

file_format: 1
