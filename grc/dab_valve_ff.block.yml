# auto-generated by grc.converter

id: dab_valve_ff
label: Valve
category: '[DAB]'

parameters:
-   id: closed
    label: Closed
    dtype: raw
-   id: feed_with_zeros
    label: Feed with zeros
    dtype: raw
    default: ' false'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.dab as dab
    make: dab.valve_ff(${closed}, ${feed_with_zeros})

file_format: 1
