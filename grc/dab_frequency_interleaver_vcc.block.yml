# auto-generated by grc.converter

id: dab_frequency_interleaver_vcc
label: Frequency interleaver
category: '[DAB]'

parameters:
-   id: interleaving_sequence
    label: Interleaving sequence
    dtype: raw
-   id: size
    label: Size
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ size }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ size }

templates:
    imports: import grdab
    make: grdab.frequency_interleaver_vcc(${interleaving_sequence})

file_format: 1
