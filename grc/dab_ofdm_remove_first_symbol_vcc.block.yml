# auto-generated by grc.converter

id: dab_ofdm_remove_first_symbol_vcc
label: OFDM Remove First Symbol
category: '[DAB]'

parameters:
-   id: num_carriers
    label: Num Carriers
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ num_carriers }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ num_carriers }

templates:
    imports: import gnuradio.dab as dab
    make: dab.ofdm_remove_first_symbol_vcc(${num_carriers})

file_format: 1
