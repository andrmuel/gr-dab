# auto-generated by grc.converter

id: dab_complex_to_interleaved_float_vcf
label: Complex to interleaved float
category: '[DAB]'

parameters:
-   id: length
    label: Length
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ length }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ length*2 }

templates:
    imports: import gnuradio.dab as dab
    make: dab.complex_to_interleaved_float_vcf(${length})

file_format: 1
