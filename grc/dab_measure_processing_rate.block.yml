# auto-generated by grc.converter

id: measure_processing_rate_x
label: DAB Measure Proc. Rate
category: '[DAB]'

parameters:
-   id: in_type
    label: Type
    dtype: enum
    default: complex
    options: [complex, float, short, byte]
    option_attributes:
        in_len: ['8', '4', '2', '1']
    hide: part
-   id: samples_to_count
    label: Samp. to count
    dtype: int
    default: '2048000'

inputs:
-   domain: stream
    dtype: ${ in_type }

templates:
    imports: import gnuradio.dab as dab
    make: dab.measure_processing_rate(${in_type.in_len}, ${samples_to_count})

file_format: 1
