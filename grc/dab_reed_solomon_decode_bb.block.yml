# auto-generated by grc.converter

id: dab_reed_solomon_decode_bb
label: 'DAB: Reed Solomon Decoder'
category: '[DAB]'

parameters:
-   id: bit_rate_n
    label: bit rate / 8 kbit/s
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import gnuradio.dab as dab
    make: dab.reed_solomon_decode_bb(${bit_rate_n})

file_format: 1
