# auto-generated by grc.converter

id: dab_time_deinterleave_ff
label: Time Deinterleaver
category: '[DAB]'

parameters:
-   id: vector_length
    label: Vector_length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: scrambling_vector
    label: Scrambling_vector
    dtype: raw

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vector_length }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vector_length }

templates:
    imports: import gnuradio.dab as dab
    make: dab.time_deinterleave_ff(${vector_length}, ${scrambling_vector})

file_format: 1
