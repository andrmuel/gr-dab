# auto-generated by grc.converter

id: dab_repartition_vectors
label: repartition_vectors
category: '[DAB]'

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: vlen_in
    label: Length in
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vlen_out
    label: Length out
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: multiply
    label: Multiply
    dtype: int
-   id: divide
    label: Divide
    dtype: int

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen_in }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen_out }

templates:
    imports: import gnuradio.dab as dab
    make: dab.repartition_vectors(${type.size}, ${vlen_in}, ${vlen_out}, ${multiply},
        ${divide})

file_format: 1
