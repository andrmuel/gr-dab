# auto-generated by grc.converter

id: dab_mp4_decode_bs
label: 'DAB: MP4 Decoder'
category: '[DAB]'

parameters:
-   id: bit_rate_n
    label: Bitrate / 8kbit/s
    dtype: int

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: raw
    multiplicity: '2'

templates:
    imports: import gnuradio.dab as dab
    make: dab.mp4_decode_bs(${bit_rate_n})

file_format: 1
