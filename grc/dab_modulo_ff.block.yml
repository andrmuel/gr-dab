# auto-generated by grc.converter

id: dab_modulo_ff
label: DAB Modulo_FF
category: '[DAB]'

parameters:
-   id: div_f
    label: Divider
    dtype: float

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.dab as dab
    make: dab.modulo_ff(${div_f})

file_format: 1
