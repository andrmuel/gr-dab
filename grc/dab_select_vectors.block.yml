# auto-generated by grc.converter

id: dab_select_vectors
label: select_vectors
category: '[DAB]'

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: total_length
    label: Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: num_msc_syms
    label: Num msc syms
    dtype: int
-   id: num_fic_syms
    label: Num fic syms
    dtype: int

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ total_length }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ total_length }

templates:
    imports: import gnuradio.dab as dab
    make: dab.select_vectors(${type.size}, ${total_length}, ${num_msc_syms}, ${num_fic_syms})

file_format: 1
