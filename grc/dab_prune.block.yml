# auto-generated by grc.converter

id: dab_prune
label: Prune
category: '[DAB]'

parameters:
-   id: itemsize
    label: Itemsize
    dtype: raw
-   id: length
    label: Length
    dtype: raw
-   id: prune_start
    label: Prune_start
    dtype: raw
-   id: prune_end
    label: Prune_end
    dtype: raw

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import grdab
    make: grdab.prune(${itemsize}, ${length}, ${prune_start}, ${prune_end})

file_format: 1
