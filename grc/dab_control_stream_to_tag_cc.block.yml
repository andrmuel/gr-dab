# auto-generated by grc.converter

id: dab_control_stream_to_tag_cc
label: control_stream_to_tag_cc
category: '[DAB]'

parameters:
-   id: vlen
    label: vlen
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: tag_str
    label: Stream tag
    dtype: string
    default: '"dab_sync"'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   label: in_control
    domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import gnuradio.dab as dab
    make: dab.control_stream_to_tag_cc(${tag_str}, ${vlen})

file_format: 1
