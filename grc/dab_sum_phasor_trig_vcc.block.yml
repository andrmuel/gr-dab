# auto-generated by grc.converter

id: dab_sum_phasor_trig_vcc
label: sum_phasor_trig_vcc
category: '[DAB]'

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import grdab
    make: grdab.sum_phasor_trig_vcc(${length})

file_format: 1
