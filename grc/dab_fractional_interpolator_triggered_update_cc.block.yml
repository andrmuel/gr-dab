# auto-generated by grc.converter

id: dab_fractional_interpolator_triggered_update_cc
label: Frac. Interp. Trig. Update
category: '[DAB]'

parameters:
-   id: phase_shift
    label: Phase Shift
    dtype: float
    default: '0'
-   id: interp_ratio
    label: Interp. Ratio
    dtype: float
    default: '1'

inputs:
-   domain: stream
    dtype: complex
-   label: sync
    domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gnuradio.dab as dab
    make: dab.fractional_interpolator_triggered_update_cc(${phase_shift}, ${interp_ratio})

file_format: 1
