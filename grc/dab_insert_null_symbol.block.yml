# auto-generated by grc.converter

id: dab_insert_null_symbol
label: insert_null_symbol
category: '[DAB]'

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import gnuradio.dab as dab
    make: dab.insert_null_symbol(${ns_length}, ${symbol_length})

file_format: 1
