# auto-generated by grc.converter

id: dab_ofdm_coarse_frequency_correct
label: OFDM coarse freq. corr.
category: '[DAB]'

parameters:
-   id: fft_length
    label: Length
    dtype: int
-   id: num_carriers
    label: Carriers
    dtype: int
-   id: cp_length
    label: CP Len
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ fft_length }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ num_carriers }

templates:
    imports: import gnuradio.dab as dab
    make: dab.ofdm_coarse_frequency_correct(${fft_length}, ${num_carriers}, ${cp_length})

file_format: 1
