# auto-generated by grc.converter

id: dab_msc_decode
label: 'DAB: MSC decoder'
category: '[DAB]'

parameters:
-   id: dab_params
    label: DAB parameter
    dtype: raw
-   id: address
    label: subchannel address
    dtype: int
-   id: size
    label: Subchannel size
    dtype: int
-   id: protection
    label: Protection mode
    dtype: int
    options: ['0', '1', '2', '3']
    option_labels: [A1, A2, A3, A4]
-   id: verbose
    label: Verbose
    dtype: bool
    options: ['True', 'False']
-   id: debug
    label: Debug
    dtype: bool
    options: ['True', 'False']

inputs:
-   label: data
    domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import gnuradio.dab as dab
    make: dab.msc_decode(${dab_params}, ${address}, ${size}, ${protection}, ${verbose},
        ${debug})

file_format: 1
