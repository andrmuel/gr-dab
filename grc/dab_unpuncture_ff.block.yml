# auto-generated by grc.converter

id: dab_unpuncture_ff
label: Unpuncture
category: '[DAB]'

parameters:
-   id: puncturing_vector
    label: Puncturing vector
    dtype: raw
-   id: fillval
    label: Fillvalue
    dtype: float

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import gnuradio.dab as dab
    make: dab.unpuncture_ff(${puncturing_vector}, ${fillval})

file_format: 1
