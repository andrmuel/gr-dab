# auto-generated by grc.converter

id: dab_xrun_monitor_ff
label: xrun_monitor
category: '[DAB]'

parameters:
-   id: length
    label: length
    dtype: int
    default: '500000'
-   id: drop_when_full
    label: Drop when full
    dtype: bool
    default: 'False'
-   id: type
    label: IO Type
    dtype: enum
    options: [complex]
    option_attributes:
        const_type: [complex_vector]
        fcn: [cc]
    hide: part

inputs:
-   domain: stream
    dtype: ${ type }

outputs:
-   domain: stream
    dtype: ${ type }

templates:
    imports: import gnuradio.dab as dab
    make: |-
        dab.xrun_monitor_${type.fcn}(${length})
        self.${id}.set_drop_when_full(${drop_when_full})

file_format: 1
