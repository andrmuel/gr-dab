# auto-generated by grc.converter

id: dab_magnitude_equalizer_vcc
label: Magnitude Equalizer
category: '[DAB]'

parameters:
-   id: vlen
    label: Number of carriers
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: num_symbols
    label: Symbols
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import gnuradio.dab as dab
    make: dab.magnitude_equalizer_vcc(${vlen}, ${num_symbols})

file_format: 1
